{
    "private": true,
    "scripts": {
        "dev": "vite",
        "dev:php": "docker-compose exec web sh -c \"cd /var/www/html/ && php artisan serve --host=0.0.0.0 --port=8000\"",
        "make:controller": "docker-compose exec web sh -c \"cd /var/www/html/ && php artisan make:controller\"",
        "build": "vite build"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.7.2",
        "lodash": "^4.17.19",
        "postcss": "^8.1.14",
        "vite": "^4.0.0"
    }
}
